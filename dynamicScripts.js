"use strict"

// помещаем скрипт в документ

const script = document.createElement('script');
script.src = "js/test.js";
document.body.append(script);

// он начнёт загружаться после  того как он будет доьавлен в документ